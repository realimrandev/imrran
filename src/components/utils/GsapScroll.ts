// Original template used GSAP timelines to sync 3D character animations with scroll.
// This version removes the dependency on GSAP trial plugins and keeps exports
// that other modules expect.

export function setCharTimeline(..._args: any[]) {
  // No-op
}

export function setAllTimeline(..._args: any[]) {
  // No-op
}
